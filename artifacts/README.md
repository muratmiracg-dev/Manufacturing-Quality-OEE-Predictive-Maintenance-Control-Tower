# Pipeline artifacts

- `results/`: metric JSON, analytical CSVs, SPC outputs, predictions, SHAP and
  maintenance-priority tables.
- `figures/`: charts rendered only from the committed metric outputs.
- `model/`: the calibrated champion bundle used by the recommendation API.

`pipeline_metrics.json` is the single source for reported portfolio metrics.

